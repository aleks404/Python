#Задача1
name = input("Enter your name:")
surname = input("Enter your surname:")
email=input("Enter your E-mail:")
print(name)
print(surname)
print(email)

#Задача2
a=int(input("time:"))
h=a//3600
m=(a//60)%60
s=a%60
print(str(h)+':'+str(m)+':'+str(s))

#Задача3
number = input ("Введите число:")
print (int (число) + int (число + число) + int (число + число + число))

#Задача4
numb = int(input("Введите число: "))
i = 10000000
max = numb % i
while numb % i < numb:
    if (numb % i) // (i / 10) > max:
        max = (numb % i) // (i // 10)
        if max == 10000000:
            break
    i *= 10
print(max)

#Задача5
прибыль = int (ввод («Финансовая прибыль Фирмы в местной валюте:»))
cost = int (ввод ("За издержки Фирмы в местной валюте:"))
результат = прибыль - затраты
если результат> 0:
    print ("Фирма отработала с прибылью")
    print (f "Рентабельность составила {результат / прибыль: .2f}")
    Распечатать()
    Сотрудники = int (input ("Количество сотрудников в Фирме:"))
    Распечатать()
    print (f "Прибыль Фирмы в расчете на каждого сотрудника составили {результат / сотрудников: .2f} PPE")
    печать ("СИЗ - Прибыль на одного работника")
elif результат <0:
    печать ("Фирма отработала в убыток")
еще:
    print ("Фирма отработала без прибыли и убытка")
    
    #Задача6
    start = int (input ("Результат в первый день, километров:"))
finish = int (input ("Необходимо достигнуть, километров:"))
день = 1
в то время как True:
    если начало> конец:
        сломать
    начало * = 1.1
    день + = 1
print (f "на {день} -й день спортсменов результата - не менее {финиш} км.")
